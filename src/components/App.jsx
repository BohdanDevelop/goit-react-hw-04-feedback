import Feedback from './Feedback';
import Section from './Section';

export const App = () => {
  return (
    <div>
      <Section title="Feedback section">
        <Feedback />
      </Section>
    </div>
  );
};
